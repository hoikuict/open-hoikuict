# 2026年9月13日 実機デプロイ完了

## 04:42 追記：サイドメニューの追加

**04:42:12 JST** に `08ec0340b7b32cc75ac94423b20c21b79f9cdeea` への追加更新が完了した。現在の実機はこのコミットで稼働している。

- 職員サイドメニューの「連絡と記録」に「文書の確認依頼」を追加。一覧と詳細画面で選択状態を表示する。
- 配備前にPC 1280px・スマートフォン390pxでリンク遷移と選択表示を確認。配備後も公開サイトのHTMLに新しいリンクが含まれることを確認した。
- `/healthz` はHTTP 200・`status=ok`。職員・保護者ログイン画面もHTTP 200。文書確認依頼は未認証時に職員ログインへ遷移する。
- 既存データ47テーブルを照合し、一致。Composeと既存設定を保持。DBバックアップとZFSスナップショット取得済み。
- 完了証跡：SSHユーザーのホームの `sidebar-20260913-08ec034/status.json`。バックアップ識別子：`before-review-sidebar-20260913-20260912T193853Z`。
- イメージID：`sha256:9ce584bb916f9c5c842a7d3af72fbc3a4e1e81bd942b2dd0629859fbb366dfd7`。

以下は同日04:19の初回更新記録。

## 04:19 仕様変更の反映

9月11日に実装した仕様変更を、2026年9月13日 **04:19:13 JST** に実機へ反映した。

| 項目 | 確認結果 |
|---|---|
| 配備コミット | `99404528a1086336f59bfd7082910c8015622c1c` |
| 更新前コミット | `e480ef125c0cf8c35b3705b6181004a5bc5267d4` |
| 更新処理の結果 | `complete` |
| アプリ・公開接続 | アプリhealthy、公開接続用コネクター稼働 |
| 既存データ | 43テーブルを更新前後で照合し、一致 |
| 構成 | Compose・認証・SMTP・通知等の既存設定を保持 |
| バックアップ | DBバックアップとZFSスナップショットを取得済み |

イメージID：

```text
sha256:6a2d7f896dfbbab3a3086ba11dbca86eeaae623cb4304ef214174a56389fbf86
```

## 更新方法

前回の起動方法を変更し、systemdの一時サービスに更新処理を監督させた。sudoの権限・監査設定は変更していない。今回の実行では、前回失敗した子プロセス起動と実機確認を通過した。

転送済みソースと更新プログラムのハッシュ確認、新イメージの作成、ネットワークを遮断したDBコピーでの起動検証、バックアップ、実機切替、既存データと構成の照合を実施した。配備済みコード288ファイル、追加5テーブル、テンプレートと本番用のセキュリティ設定も更新プログラムで確認した。

途中で管理端末に起動エラーが表示されたが、実機の更新サービス本体は稼働していた。再起動せずに状態を追跡し、`complete` と新しい配備コミットを確認した。

## 更新後の確認

ブラウザーから、データを変更しない方法で確認した。

- `/healthz`：HTTP 200、`status=ok`。
- `/staff/login`、`/parent-portal/login`：HTTP 200。
- `/document-reviews/`、`/settings/guardian-terminals`：未認証の場合は職員ログイン画面へ遷移。
- 存在しないURL：HTTP 404のHTML案内を表示。

実際の保護者へのテスト通知、実記録の打刻取消、物理タブレットの通信断試験は実施していない。機能の操作確認は配備前の架空データによる自動テスト・ブラウザー検証で実施済み。

## 運用記録

実機のSSHユーザーのホームにある `spec-20260913-9940452/status.json` が完了証跡。バックアップとスナップショットの所在も記録している。今回のバックアップ識別子は `before-spec-improvements-20260911-20260912T191544Z`（時刻部分はUTC）。内部接続先・認証情報・個人別レコードはこの文書に含めていない。

実装内容は [9月11日の仕様変更](spec-improvements-2026-09-11.md)、中断時の経緯は [引き継ぎ記録](handoff-2026-09-11-spec-deployment.md) を参照。今回のデプロイは完了しているため、中断時の更新コマンドを再実行する必要はない。
