# 家族削除機能：配備記録（2026-09-21）

状態：配備ファイルの実機転送と事前検査まで完了。sudoの対話パスワード入力待ちで、本番切替は未実施。

ユーザーは確認用モック試用後に「実装してみよう」と指示し、実装後に「では実機っで確認しよう」と実機確認を依頼した。

## 配備する版

- 本番基準：`66471bffb6eb3bba0b1783a498b84dad7fbaade6`。実機のdeployment.json・Composeのハッシュを照合済み。
- 配備候補：`4b7f9be3069b5d46330824d5a4270c8a6a2028b4`。
- push済みブランチ：`codex/family-delete-20260921-production`。
- ソース：`.local-dev/truenas-family-delete-20260921/source`。
- 開発ブランチ全体の配備は行わず、既存のバックアップ形式1・gateway・復元ワーカー・設定メニューを保持する。

## 機能と検査

家族一覧の「削除」から内容・家族番号・関連記録を確認する。園児台帳管理権限が必要。関連園児、保護者アカウント、請求、回答、写真、初回入力依頼などがあれば削除を拒否する。削除前に確認チェック・CSRF・署名情報・最新の権限と関連記録を検査する。DBスキーマ変更なし。

実装テストは重複を除く81件が成功。架空データでブラウザー操作・モックとの照合を完了。実機確認用の読み取り専用検査も架空データで実行し、削除可能な家族と関連記録で保護される家族を正しく区別した。

配備手順の4テストで、設定保持、意図しないアクセス追加の拒否、復元・バックアップ中の切替拒否、正常切替、起動失敗・検証失敗・部分停止からの旧アプリと両ワーカーの再開を検証した。実機のLinuxでも実行済み。転送ファイルの全ハッシュとPython構文を照合済み。

## 実機で実行する内容

固定commitのイメージをビルドし、ネットワークを無効にしたコンテナで関連81テストと復元・切り戻しを検証する。現行DBコピーで既存機能・スキーマ保持を検査し、保存済みバックアップの互換性も検査する。

成功後に公開接続を短時間止め、バックアップ完了を待ち、復元キューロック下でジョブがないことを確認してアプリと両ワーカーを停止する。ZFS snapshot・主DB・施設文例DB・設定を退避し、新版を起動する。既存のCompose内容は変えず、イメージ情報と復元可能commitの追記だけを許可する。復元用署名鍵・既存サービス・設定値は保持する。

切替後は既存機能に加え、実機の全家族について一覧・削除確認画面・関連記録による拒否・閲覧権限の拒否を読み取り専用で検査する。出力は件数だけで、家族名・連絡先・トークンは出力しない。72テーブル、添付、定期バックアップ設定の保持を検査して公開接続を再開する。

失敗時は旧イメージと設定、既存のbackup-worker・restore-workerを再開する。実データの削除や過去バックアップからの復元は、この配備・検証では行わない。

## 再開する場所

- 実機の準備済みディレクトリ：`/home/truenas_admin/family-delete-20260921-4b7f9be`。
- 起動：同ディレクトリの `start-systemd.py` をsudoで1回実行する。
- Unit：`hoikuict-family-delete-20260921-4b7f9be`。
- 起動後は同ディレクトリの `status.json` を確認し、再起動コマンドを重ねて実行しない。
- ヘルパー・manifest・転送記録：`.local-dev/truenas-family-delete-20260921/bundle`。

パスワードやsudoの設定を変更して自動実行することはしない。ユーザーによる対話sudo後に完了状態・本番の版・家族画面の検査結果を照合し、この記録を完了に更新する。
