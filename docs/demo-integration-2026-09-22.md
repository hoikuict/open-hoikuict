# デモブランチへの反映（2026-09-22）

対象：`demo/public-demo-dockge-cloudflare`。ユーザーの「コミットとプッシュ」「デモブランチでモック認証を維持したまま、ここまでの変更点を反映」に対応するコード統合。

## 取り込んだ変更

- 本番反映済み `5dec9cf39da19f1ddcff82eadeaa0fac85bac162` を統合。家庭一覧だけのアーカイブ、未使用家庭の削除保護、設定メニュー、職員ログイン時間、9月15〜19日の業務画面更新を含む。
- `dc2dd95` の保護者CSV省略・姓名編集の修正、合意したモック、アーカイブ配備記録を取り込んだ。
- バックアップ形式は本番基準の形式1を保持。公開デモではホスト共通のバックアップ・復元操作を無効にし、設定画面にも表示しない。

## デモとして維持した動作

`PUBLIC_DEMO_MODE=1` では、職員・保護者ともモック認証を使う。パスワード認証用の環境値が存在していても、公開デモでは本人選択を維持し、認証メールのワーカーを起動しない。

利用者ごとのSQLite、期限による破棄、書込上限、Web Pushのセッション分離を保持。アーカイブ・削除の確認トークンとCSVの確認データもデモセッションに結びつけ、同じ架空職員を選んだ別利用者からの再利用を拒否する。

配布用の架空DBはそのまま保持し、起動時のコピーにアーカイブ・写真・お迎え確認等の不足列を追加する。旧データの保護者番号が文字列で保存されている場合も、家庭CSVの差分確認でエラーにならないようにした。

## 検証

- アプリ全体：868件成功（432.62秒）。CIと同じ開発用モック認証・キオスク設定で実行。
- デモ互換・家庭アーカイブ・保護者情報保持の対象43件も成功。モック認証、アーカイブ・復帰後の保護者画面、確認トークンやCSVの利用者間再利用拒否、旧デモDBの全モデル列を検査した。
- `ruff check .` 成功。
- コンバーターのブラウザーテスト成功。姓名を省略する操作、連続入力時のフォーカス、保存したCSVへの反映、取消、24列、外部通信なしを確認。
- アーカイブモックのビルド成功。固定テンプレートと既存27種類のフォーム項目を維持。

全体テストの初回は既存のWindows一時ディレクトリの権限で停止した。ワークスペース内の新規専用ディレクトリを指定して上記868件の完走を確認した。

統合コミットは `0008534`、コンバーター・記録の取り込みは `5cc1fa6`。作業ブランチ `codex/spec-20260919` には元の変更を `dc2dd95` として保存し、資料目次の補足を `fa18b79` としてプッシュした。

この作業はブランチへのコード反映。デモサーバーの再配備や本番環境の追加更新は行わない。
