# 2026年9月19日の本番更新記録

状態: **2026年9月19日12:29:00 JSTに本番反映完了。**

ユーザーによるsudo認証後、更新プログラムが正常に完了した。完了証跡と現在の配備メタデータを照合し、版・イメージの一致とCompose設定の保持を確認済み。起動コマンドの再実行は不要。

## 固定した配備版

- 更新前: `bba95bbd1226fdae1b2362d4ca28a8bc91292e0a`（9月17日版）。
- 配備版: `8e458f223e2b32b30675cfec78cb2464fcf1fc0e`。
- 本番用ブランチ: `codex/spec-20260919-production`。
- 開発側の実装: `fd45db65c5fdc62c1e63c552c6306833d3513eaf`（`codex/spec-20260919`）。

[モックで確認したUI](spec-review-2026-09-19.md)と、朝食・食欲のボタンを入力欄直下へ移動する修正を含む。園別の閉園時刻設定・候補制限は含めない。本番の既存機能・バックアップ方式・Compose・認証・通知設定を保持する。開発側だけにあるバックアップv2は配備しない。DB変更は `attendance_records` の2列追加。

## 検証結果

- 配備版の全体テスト: **749件成功**、295.28秒。
- 開発版は773件成功。朝食ボタン移動後の関連テストも成功。
- Ruff、MkDocs strict、差分の空白検査: 成功。
- 固定した配備版をEdgeで操作し、日次連絡の保存、登園の確認・修正・取消・決定、降園時の実際のお迎えの人、目視確認からの降園、全画面維持を確認。
- 320〜1280pxの表示と朝食ボタンの位置を確認。検証は架空データのみ。
- 更新プログラムの構成保持、WALバックアップ、既存データ改変検出、正常更新、切替前中断、復旧をローカルで模擬検証済み。
- 移行・スキーマ・既存機能確認コードをローカル合成DBで検証済み。
- 実機へ転送後、SHA256・Python構文・更新前の版と構成の不変・更新未開始を確認。
- ランタイム314ファイルをハッシュ固定。更新処理では既存68テーブルの内容を照合する。

公開HTTPSへの自動取得は、更新前後とも `/healthz`、職員・保護者ログインが403。Access設定は変更していない。実機の公開画面を確認済みとは扱わず、更新処理内のヘルスチェックとスキーマ・ファイル・既存データの照合、および現在の配備メタデータとの一致を完了根拠とする。

## 本番反映の確認

更新プログラムが新イメージを構築し、ネットワークを遮断した実DBコピーで移行を反復検証した。切替前にSQLiteバックアップ、設定退避、ZFSスナップショットを取得。更新後にアプリの正常起動、コネクターの稼働、既存68テーブルの内容、追加2列、ランタイム314ファイル、Compose設定の保持を確認した。

- 配備イメージ: `sha256:d2cc8f4f9fc8a3ca97bc916c020e14de67b764aeb4b5f9d6acfa26094a5ccf4e`。
- 更新前バックアップ・スナップショット識別子: `before-spec-20260919-20260919T032608Z`。
- 完了証跡: SSHユーザーのホームの `spec-20260919-8e458f2/status.json`。
- 完了状態は `complete`。現在の配備メタデータが完了証跡の版・イメージと一致することを再確認した。

実利用者のアカウントを使った打刻・連絡・編集試験は行っていない。利用中の共用端末・保護者画面は一度再読み込みしてから使用する。
