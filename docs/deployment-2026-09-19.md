# 2026年9月19日の本番更新準備

状態: **配備準備・転送・検証完了。実機のsudo認証待ち。更新は未開始。**

ユーザーは「実機に反映してください」と依頼済み。更新自体の再承認は不要。2026年9月19日12:23 JSTに更新プログラムの起動を試みたが、実機の `sudo` がパスワードを要求した。状態確認は `not_started` で、稼働アプリとDBは変更していない。

## 固定した配備版

- 更新前: `bba95bbd1226fdae1b2362d4ca28a8bc91292e0a`（9月17日版）。
- 配備対象: `8e458f223e2b32b30675cfec78cb2464fcf1fc0e`。
- 本番用ブランチ: `codex/spec-20260919-production`。
- 開発側の実装: `fd45db65c5fdc62c1e63c552c6306833d3513eaf`（`codex/spec-20260919`）。

[モックで確認したUI](spec-review-2026-09-19.md)と、朝食・食欲のボタンを入力欄直下へ移動する修正を含む。園別の閉園時刻設定・候補制限は含めない。本番の既存機能・バックアップ方式・Compose・認証・通知設定を保持する。開発側だけにあるバックアップv2は配備しない。DB変更は `attendance_records` の2列追加。

## 検証結果

- 配備版の全体テスト: **749件成功**、295.28秒。
- 開発版は773件成功。朝食ボタン移動後の関連テストも成功。
- Ruff、MkDocs strict、差分の空白検査: 成功。
- 固定した配備版をEdgeで操作し、日次連絡の保存、登園の確認・修正・取消・決定、降園時の実際のお迎えの人、目視確認からの降園、全画面維持を確認。
- 320〜1280pxの表示と朝食ボタンの位置を確認。検証は架空データのみ。
- 更新プログラムの構成保持、WALバックアップ、既存データ改変検出、正常更新、切替前中断、復旧をローカルで模擬検証済み。
- 移行・スキーマ・既存機能確認コードをローカル合成DBで検証済み。
- 実機へ転送後、SHA256・Python構文・更新前の版と構成の不変・更新未開始を確認。
- ランタイム314ファイルをハッシュ固定。更新処理では既存68テーブルの内容を照合する。

公開HTTPSへの自動取得は、更新前から `/healthz`、職員・保護者ログインとも403。アプリ停止とは判断せず、Access設定も変更しない。認証後は更新プログラム内のヘルスチェックとスキーマ・ファイル・既存データの照合で確認する。

## 認証後の処理

更新プログラムは新イメージの構築、ネットワークを遮断した実DBコピーでの移行検証、バックアップとZFSスナップショット、短時間の停止・切替・稼働確認を行う。設定とバックアップは実機内に保持する。

起動コマンドを再実行する前に、専用パッケージの `status.json` を確認する。起動エラーでも処理が始まっている可能性を確認し、二重に起動しない。`complete` を確認してから、この記録を反映完了へ更新する。
