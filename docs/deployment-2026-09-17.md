# 2026年9月17日の本番更新記録

状態: **配備準備完了、サーバーのsudo認証待ち。本番の切替はまだ実施していない。**

## 対象版

- 現在の本番: `53967256246ffa238dc7aa6d45914393a31a0f4c`（9月16日版）。
- 配備予定版: `bba95bbd1226fdae1b2362d4ca28a8bc91292e0a`。
- 配備用ブランチ: `codex/spec-20260917-production`。
- 開発側の実装コミット: `37f2a29`（`codex/spec-20260917`）。

本番版を土台に[9月17日の17項目](spec-improvements-2026-09-17.md)を統合した。開発側の作業ツリーには本番の既存機能を戻してしまう差分があったため、そのまま配備せず、カレンダー取込・議事録のWord/Markdown出力・端末全画面維持等を保持した。

今回の本番配備には開発側のバックアップv2を含めない。現行本番のSQLite全体バックアップとCompose・認証・通知設定を保持する。DB変更は3テーブルと、お迎え変更履歴の4列追加である。既存アンケートの結果閲覧は管理者のみから始まり、必要な職員を管理者が指定する。

## 配備前の検証

- 本番統合版の全体テスト: **722件成功、372.62秒**。
- 開発版の全体テスト: **746件成功、578.96秒**。配備版とは対象が異なる。
- Ruff、MkDocs strict、差分の空白検査: 成功。
- Edgeで今日の機能と既存のICS取込・議事録出力・端末の全画面維持と通信復帰を合成データで確認。
- 更新ヘルパーの構成保持、WALバックアップ、データ改変検出、正常更新、中断、復帰の模擬検証: 成功。
- 配備先で実行するスキーマ・機能確認コードをローカル合成DBで検証: 成功。
- 転送後のSHA256、Python構文、本番の版と設定の不変: 確認済み。
- 配備対象のランタイムファイル310件のハッシュを固定。

更新前の公開HTTPS自動アクセスは `/healthz`、職員・保護者ログインとも403だった。前回と同様、公開応答だけでアプリ停止とは判断しない。

## 認証後に行う確認

更新プログラムは新イメージを構築し、ネットワークを遮断した実DBコピーで移行を反復検証する。切替前にSQLiteバックアップ・設定退避・ZFSスナップショットを取得する。更新後は既存65テーブルの内容、追加スキーマ、ランタイムファイル、Compose設定、アプリのヘルスとコネクターを照合する。

この時点では実DBコピーでのリハーサル、本番バックアップ、切替、更新後確認は未実施。認証後の完了証跡に合わせてこの文書を更新する。
