# UIの変更を進める手順

ユーザーは、細かい画面仕様の認識違いを減らすため、仕様確認・モック試用・実装の順で進めることを希望している。

1. 変更書を読み、要望、こちらの解釈、未確定事項を分ける。画面の表示条件、遷移、保存するタイミング、修正・取消の動作を確認する。
2. 現行の配備版を基準に、架空データで操作できるモックを作る。対象画面の既存入力項目を省略せず、変更後の入力順と画面全体の長さも確認できるようにする。未入力・入力済み・一部入力・エラーを切り替えて試せるようにする。モックは本番DBや送信処理に接続しない。
3. ユーザーがモックを試して認識を合わせてから、合意した内容を実装する。モックの作成依頼を、そのまま本実装や配備の依頼とは扱わない。
4. 実装後は合意したモックと動作を照合し、必要なテストを実行する。配備はユーザーの依頼に従って進める。

確認のための画面と業務画面を区別し、未確定の提案を確定仕様として報告しない。ユーザーが別の進め方を明示した場合は、その指示を優先する。

配備作業では最新の更新記録を確認する。開発ブランチと本番用ブランチの機能が異なる場合があるため、既存の本番機能を失う更新をしない。
